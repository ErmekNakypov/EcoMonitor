@model EcoMonitor.Web.Models.Air.AirQualitySummaryViewModel
@using System.Globalization
@using EcoMonitor.Web.Helpers

@{
    var color = AqiHelper.GetColorHex(Model.AqiLevel);
    var label = AqiHelper.GetLabel(Model.AqiLevel);
    var darkText = Model.AqiLevel == AqiLevel.Moderate;
}

<div class="air-summary-card mb-4" style="border-left: 4px solid @color">
    <div class="air-summary-card__label">Air quality right now</div>
    <div class="d-flex align-items-baseline gap-2 mt-2">
        <span class="air-summary-card__value">@(Model.AverageAqiUs.HasValue ? Math.Round(Model.AverageAqiUs.Value).ToString("0", CultureInfo.InvariantCulture) : "—")</span>
        <span class="text-muted small">AQI US (average)</span>
        <span class="aqi-pill ms-1" style="background:@color; color:@(darkText ? "#78350F" : "#fff")">@label</span>
    </div>
    <div class="text-muted small mt-2">@Model.ActiveStations of @Model.TotalStations stations active in the last 24 hours</div>
    <div class="mt-3">
        <a href="/Map" class="btn btn-sm btn-outline-secondary">
            <i class="bi bi-geo-alt me-1"></i>View map
        </a>
    </div>
</div>
